### FortiGate HA Configuration
### Generated from spec — do not edit manually
###
config system global
    set hostname "{{ hostname }}"
end

{% for iface in interfaces %}
config system interface
    edit "{{ iface.name }}"
{% if iface.get('ipv4') %}
        set ip {{ ip_no_mask(iface.ipv4) }} {{ cidr_to_subnet(ip_mask(iface.ipv4) | int) }}
{% endif %}
        set allowaccess ping https ssh
        set description "{{ iface.description | default('') }}"
{% for zone in zones if iface.name in zone.get('interfaces', []) %}
        set zone "{{ zone.name }}"
{% endfor %}
    next
end

{% endfor %}
config system ha
    set mode a-p
    set group-id {{ ha_group_id }}
    set group-name "{{ site_key }}-ha-cluster"
    set priority {{ ha_priority }}
    set hbdev "port3" 50
    set session-pickup enable
    set override disable
end

{% for zone in zones %}
config system zone
    edit "{{ zone.name }}"
        set intrazone allow
        set description "{{ zone.type }} zone"
    next
end

{% endfor %}
{% for policy in policies %}
config firewall policy
    edit {{ loop.index }}
        set name "{{ policy.name }}"
        set srcintf "{{ policy.source_zone }}"
        set dstintf "{{ policy.destination_zone }}"
        set srcaddr "all"
        set dstaddr "all"
        set action {{ policy.action }}
{% if policy.get('services') %}
        set service{% for svc in policy.services %} "{{ svc }}"{% endfor %}

{% endif %}
        set schedule "always"
        set logtraffic all
    next
end

{% endfor %}
end
